# Expo/React Native pulls react-dom + radix + vaul (for web support) as
# optional/peer deps. Their peer ranges can be stricter than our pinned RN
# versions, so allow npm (and EAS Build's `npm ci`) to install despite peer
# mismatches instead of hard-failing.
legacy-peer-deps=true
